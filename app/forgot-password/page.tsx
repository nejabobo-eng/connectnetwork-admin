'use client'
import { FormEvent, useState } from 'react'

export default function ForgotPasswordPage() { const [message, setMessage] = useState(''); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const response = await fetch('/api/auth/recover', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(Object.fromEntries(new FormData(event.currentTarget))) }); const data = await response.json().catch(() => ({})); setMessage(response.ok ? 'Check your email for the reset link.' : data.error || 'We could not send the reset email.') } return <main className="mx-auto flex min-h-screen max-w-md items-center px-5"><form className="w-full rounded-2xl border bg-white p-8 shadow-sm" onSubmit={submit}><h1 className="text-3xl font-bold">Reset dashboard password</h1><p className="mt-3 text-slate-600">Enter the authorised admin email address.</p><input className="mt-6 w-full rounded-lg border p-3" required name="email" type="email" placeholder="Email address" /><button className="mt-4 w-full rounded-lg bg-navy px-5 py-3 font-semibold text-white">Send reset link</button>{message && <p className="mt-4 text-sm text-slate-600">{message}</p>}</form></main> }
